_schema-version: '3.1'
ID: coffeex-cap
description: CoffeeX Smart Coffee Dispenser Backend
version: 1.0.0

modules:
  - name: coffeex-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: coffeex-hdi
      - name: coffeex-alerts
      - name: coffeex  # ✅ XSUAA auth binding added
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      memory: 512M
      disk-quota: 1024M
    build-parameters:
      builder: custom
      commands:
        - npm install --legacy-peer-deps

  - name: coffeex-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: coffeex-hdi
    parameters:
      buildpack: nodejs_buildpack
      memory: 256M
      disk-quota: 512M

  - name: coffeex-ui
    type: html5
    path: app/coffee-frontend
    build-parameters:
      builder: custom
      commands:
        - npm install
        - npm run build
      build-result: dist
      supported-platforms: []
    requires:
      - name: coffeex-srv
        group: destinations
        properties:
          name: srv-api
          url: ${srv-api-url}
      - name: coffeex  # ✅ XSUAA auth binding added
      
  - name: coffeex-approuter
    type: approuter.nodejs
    path: approuter
    parameters:
      disk-quota: 256M
      memory: 256M
    requires:
      - name: coffeex  # this is your XSUAA service instance
      - name: coffeex-Destination  # ✅ destination service bound to approuter

resources:
  - name: coffeex-hdi
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
    properties:
      hdi-service-name: ${service-name}

#Destination Service created by tutors
  - name: coffeex-Destination
    type: org.cloudfoundry.existing-service

#Destination service that we created

 # - name: coffeex-srv-api
 #   type: org.cloudfoundry.managed-service
 #   requires:        #  NEW: explicitly link to srv-api
  #    - name: srv-api
   # parameters:
    #  service: destination
     # service-plan: lite
      #config:
       # destinations:
        #  - name: coffeex-srv
         #   url: ~{srv-api/srv-url}   # now this link will resolve correctly
          #  forwardAuthToken: true


  - name: coffeex  # ✅ XSUAA service definition
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json

  # Event Mesh Service - uncomment when available
  # - name: coffeex-mesh
  #   type: org.cloudfoundry.managed-service
  #   parameters:
  #     service: enterprise-messaging
  #     service-plan: default
  #     config:
  #       emname: coffeex-messaging
  #       options:
  #         management: true
  #         messagingrest: true
  #       rules:
  #         queueRules:
  #           publishFilter:
  #             - "${namespace}/*"
  #           subscribeFilter:
  #             - "${namespace}/*"
  #         topicRules:
  #           publishFilter:
  #             - "${namespace}/*"
  #           subscribeFilter:
  #             - "${namespace}/*"

  - name: coffeex-alerts
    type: org.cloudfoundry.managed-service
    parameters:
      service: alert-notification
      service-plan: free

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install --legacy-peer-deps
        - npx cds build --profile production
